# Uncomment to run with Foreman on a machine with Redis + Postgres provisioned.
# web: bundle exec puma -C config/puma.rb
# worker: bundle exec sidekiq -C config/sidekiq.yml
# release: bundle exec rails db:migrate